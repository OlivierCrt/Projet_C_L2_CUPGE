#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>


typedef struct s_date* date;
typedef struct s_tronçon* Troncon;
typedef struct s_trajet* Trajet;
typedef struct s_ville* Ville;
typedef struct s_conducteur* Conducteur;
typedef struct s_client* Client ;

